{
  "name": "@wstera/product-billing-profile-registry",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
