You are implementing Stage 2 (Builder) of the Audit Log module for the Module Hub project.

## Working directory
D:/AI-Workspace/projects/modules-hub/modules/audit-log

## Read this first — it is the ONLY source of truth
Read the file `D:/AI-Workspace/projects/modules-hub/modules/audit-log/DESIGN.md` in full. It fully specifies the module contract, types, validation rules, redaction engine, clone/immutability semantics, error codes, AuditStore adapter interface, file structure, package/tsconfig. Implement the code EXACTLY per that design. Do not invent your own contract.

## Your deliverable — write these files to these EXACT absolute paths
1. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/core/types.ts`
   - Pure type definitions only, no implementation: AuditActor, AuditEntity, AuditEvent, AuditRecord, RecordResult, DateRangeFilter, QueryFilters, QueryResult, RedactionConfig, AuditLogConfig, AuditError, AuditErrorCode, AuditStoreQueryResult, AuditStore, PostgresAuditStoreOptions, AuditLogClient. Match §3, §4, §6, §7 exactly.
2. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/core/validate.ts`
   - Pure functions `validateEvent(event)` and `validateFilters(filters)` per DESIGN. `validateEvent` returns `{ ok: true }` or `{ ok: false, error: AuditError }` with code EVENT_INVALID. Rules: actor required (object with non-empty string `type`; `id` optional), action required non-empty string, entity required (object with non-empty string `type` AND non-empty string `id`), timestamp optional but if supplied must be a valid ISO 8601 date string (EVENT_INVALID otherwise), before/after optional unknown, metadata optional Record<string,unknown>. `validateFilters(filters)` validates filter shape (limit/offset positive integers, limit capped; dateRange.from/to valid ISO strings) returning `{ ok: true }` or `{ ok: false, error: AuditError }` with code QUERY_FAILED. Error messages must never contain sensitive data.
3. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/core/redact.ts`
   - `redactObject(data, options)` implementing §5.1 recursive traversal & field masking. Export types for RedactOptions (customSensitiveFields?: readonly string[], mask?: string). Built-in sensitive field names (case-insensitive): password, token, secret, authorization, apiKey/apikey, creditCard/creditcard, ssn, privateKey/privatekey. Merge built-in + host customSensitiveFields into a normalized lowercase Set. Recursively inspect nested objects/dicts/arrays. If a key matches a sensitive field (case-insensitive exact match): primitive values (string/number/boolean/null/undefined) replaced with the mask string; nested object/array assigned to a sensitive key replaced wholesale with the mask string. Non-sensitive keys recursively traversed. Arrays traversed element by element. Non-plain objects (Date, RegExp) and primitives passed through safely. Circular references handled safely with a WeakSet — no unhandled stack overflow. Default mask "[REDACTED]", overridable via options.mask.
4. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/core/clone.ts`
   - `deepClone(data)` providing immutability isolation per §5.2. Must deep-clone plain objects/arrays (including nested), and safely pass through non-plain objects (Date → clone via new Date(value.getTime()), RegExp → new RegExp(source, flags), Map/Set if used) so redacted output is a structurally independent deep copy. Circular references handled safely (WeakMap of seen references). The caller's input AuditEvent object must never be mutated.
5. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/core/audit.ts`
   - Implements `createAuditLog(config: AuditLogConfig): AuditLogClient` orchestrating validate → redact → clone → generate ID/timestamp → store.append → return RecordResult. Per §2, §2.1, §2.2, §5.3, §5.4. NEVER reads process.env/env/globalThis. Throws AuditError CONFIG_INVALID at construction if config malformed (missing store, invalid mask, invalid custom field names). `record(event)` NEVER throws for validation/redaction/storage failures — returns RecordResult with success:false and structured AuditError (EVENT_INVALID / REDACTION_FAILED / STORE_FAILED). Does NOT mutate caller's input AuditEvent — before/after/metadata become immutable deep-cloned redacted snapshots. id generated via Web Crypto `crypto.randomUUID()` (NO node:crypto). timestamp: use event.timestamp if valid ISO else config.getCurrentTimestamp?.() ?? new Date().toISOString(). `query(filters)` delegates to config.store.query(filters); on failure returns success:false with QUERY_FAILED or PROVIDER_ERROR. `close?()` delegates to config.store.close?.().
6. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/core/index.ts`
   - Public barrel: re-export `createAuditLog`, `AuditLogClient`, and all public types (AuditEvent, AuditRecord, AuditLogConfig, QueryFilters, QueryResult, RecordResult, AuditError, AuditErrorCode, AuditStore, AuditStoreQueryResult, AuditActor, AuditEntity, RedactionConfig, DateRangeFilter, PostgresAuditStoreOptions). Per §8.1.
7. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/adapters/memory.ts`
   - `createInMemoryAuditStore(): AuditStore` implementing §7.1. Internal private AuditRecord[] array. `append(record)` pushes a deep-cloned copy. `query(filters)` filters in-memory by actor.id & actor.type (exact match if specified), action (exact), entity.type & entity.id (exact), dateRange.from (timestamp >= from) & dateRange.to (timestamp <= to), offset (default 0), limit (default 50, capped 1000). Returns deep-cloned copies of matching records so host callers cannot mutate internal state. Returns { records, total }.
8. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/adapters/postgres.ts`
   - `createPostgresAuditStore(options: PostgresAuditStoreOptions): AuditStore` implementing §7.2. Accepts generic query executor `query: <T = unknown>(sql: string, params: unknown[]) => Promise<{ rows: T[]; count?: number }>` and tableName (default "audit_logs"). `append(record)` maps AuditRecord to the SQL DDL schema from §7.2 (id UUID, actor_id, actor_type, action, entity_type, entity_id, before JSONB, after JSONB, metadata JSONB, timestamp TIMESTAMPTZ) and INSERTs. `query(filters)` builds a parameterized SELECT with WHERE clauses for actor/action/entity/dateRange + LIMIT/OFFSET, returns { records, total }. NO raw string interpolation of user filter values — always parameterized. Include the DDL schema as an exported `AUDIT_LOG_DDL` string constant. No env reads. NO node:* imports.
9. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/package.json`
   - Use the EXACT content from DESIGN.md §11 (name audit-log-module, version 0.1.0, private true, type module, the given description, scripts typecheck: tsc --noEmit and test: vitest run, devDeps typescript 5.6.3, vitest 2.1.4).
10. `D:/AI-Workspace/projects/modules-hub/modules/audit-log/tsconfig.json`
    - Use the EXACT content from DESIGN.md §11: target ES2022, module ES2022, moduleResolution Bundler, lib [ES2022, DOM], strict, esModuleInterop, skipLibCheck, declaration, outDir dist. Include `["core", "adapters"]` (do NOT include tests or integration.example.ts — those are later stages and don't exist yet).

## Constraints
- Follow DESIGN.md exactly — do not invent your own contract.
- TypeScript, strict mode.
- Use Web Crypto API for crypto (crypto.randomUUID()). NO node:crypto / node:* imports — must run on Cloudflare Workers.
- No business-specific logic (no booking/ticket/shop/order/customer specifics). Generic terms only: actor/action/entity/before/after/metadata.
- Module NEVER reads process.env/env/globalThis — Host injects config.
- Do NOT write tests (later stage / Qwen). Do NOT write MODULE.md or integration.example.ts (later stage / Claude). Do NOT create a tests/ directory or any test files.
- Do NOT run any scaffolder, `git clean`, `rm -rf`, or anything that could delete/overwrite existing files in this folder. DESIGN.md must survive. Do not delete or modify DESIGN.md. Do not touch files outside D:/AI-Workspace/projects/modules-hub/modules/audit-log.

## When done
Write all 10 files to the exact absolute paths above. Then run `npm install` and `npm run typecheck` in the working directory D:/AI-Workspace/projects/modules-hub/modules/audit-log to confirm the code compiles with exit code 0. If typecheck fails, fix the errors until it passes. Report what you built (file list) and the real typecheck exit code.
