You are writing a comprehensive vitest test suite for a Webhook Receiver module (TypeScript, ES modules, "type": "module"). This is a QA/tests-only task. Write tests ONLY. Do NOT edit, refactor, or "fix" any production source file under core/ or providers/. If a test reveals a genuine production bug, do NOT fix it yourself — note it for the report.

# Working directory (absolute)
D:\AI-Workspace\projects\modules-hub\modules\webhook-receiver

# Read these files first (they are the ground truth — do not invent APIs)
- package.json  (test script: "vitest run"; NO test:coverage script is configured)
- tsconfig.json (strict TS, ES2022, moduleResolution Bundler)
- core/types.ts
- core/index.ts  (exports createWebhookReceiver + all types)
- core/errors.ts (exports createError, failureResult, timingSafeEqual)
- core/timestamp.ts (DEFAULT_TIMESTAMP_TOLERANCE_SECONDS=300, parseTimestamp, validateTimestampWindow)
- core/payload.ts (DEFAULT_PAYLOAD_MAX_BYTES=1MB, rawBodyToString, getRawBodyByteLength, guardPayloadSize, parseJsonPayload)
- core/idempotency.ts (extractIdempotency, runIdempotencyStore)
- core/verifier.ts (buildVerifierRegistry, resolveVerifier)
- core/verify.ts (createVerifyContext, verifyWebhookRequest, normalizeHeaders)
- providers/generic-hmac/hmac.ts (GenericHmacVerifier — real, implemented)
- providers/generic-hmac/index.ts
- providers/line/index.ts (LineWebhookVerifier — STUB: returns WEBHOOK_UNKNOWN_PROVIDER "not yet implemented")
- providers/stripe/index.ts (StripeWebhookVerifier — STUB: same)
- providers/github/index.ts (GithubWebhookVerifier — STUB: same)

# CRITICAL honesty rules about the code you must test
1. GenericHmacVerifier is the ONLY fully-implemented verifier. Exercise it genuinely: valid HMAC-SHA256 (hex, with and without "sha256=" prefix), HMAC-SHA512, base64 encoding, timestamp validation, missing/malformed/expired timestamp, missing/invalid/malformed signature, wrong secret, mismatched body, timing-safe comparison via timingSafeEqual, prefix stripping.
2. LINE, Stripe, and GitHub providers are UNIMPLEMENTED STUBS. They return WEBHOOK_UNKNOWN_PROVIDER ("not yet implemented"). You MUST still test them, but you must test their ACTUAL behavior: calling their verify() returns valid:false with code WEBHOOK_UNKNOWN_PROVIDER and provider set to the provider name. Do NOT fabricate signatures and do NOT mock them away. Do NOT write tests asserting behavior the stubs do not implement (e.g. do not assert a LINE signature validates). Be accurate to the real code.
3. createWebhookReceiver / verifyWebhookRequest (core) is the full pipeline: payload size guard -> verifier resolution (single verifier, verifiers map, defaultProvider, unknown provider) -> signature -> timestamp window -> JSON parse -> idempotency/replay extraction (eventId from header / eventIdPath / eventTypePath) -> structured result. Test happy paths AND failure paths. Test normalizeHeaders with plain object (string, array -> joined ", ", undefined skipped) and with a real Web `Headers` instance.
4. buildVerifierRegistry config validation: payloadMaxBytes<=0 throws WEBHOOK_CONFIG_INVALID, timestampToleranceSeconds<=0 throws, no verifiers throws, defaultProvider not found throws, verifier+verifiers map registration and providerName keying.
5. Idempotency/replay: use an in-memory IdempotencyStore test double (Map-based: has(key) returns boolean, set(key,ttl) records) that is a REAL implementation, not a vi.fn() mock, so replay detection is genuinely exercised. Duplicate eventId -> WEBHOOK_REPLAY_DETECTED. No store/eventId -> no replay check. TTL passed through.
6. payload: oversized body (over default 1MB AND over a custom smaller limit) -> WEBHOOK_OVERSIZED_PAYLOAD; malformed JSON -> WEBHOOK_MALFORMED_JSON; empty object {} is valid; valid JSON returns parsed payload. Test both string and Uint8Array rawBody forms.

# Test file to write (absolute output path — required)
Write EXACTLY ONE test file to this absolute path:
D:\AI-Workspace\projects\modules-hub\modules\webhook-receiver\tests\webhook.test.ts

Match the sibling style: `import { describe, it, expect } from 'vitest';`, `import { ... } from '../core';`, `import { GenericHmacVerifier } from '../providers/generic-hmac';` etc. Use describe/it. Include a header comment block. Use 2-space indentation. Keep it dependency-free (no extra packages).

# Quality bar
- Aim for 60-90 passing tests across clear describe blocks, genuinely exercising the real code.
- No empty bodies, no `.skip`, no `.todo`, no `vi.mock`, no tests that don't actually invoke the module under test.
- Cover happy paths AND failure/security paths for: request parsing (normalizeHeaders, payload parse), GenericHmac signature verification (HMAC generic), timing-safe comparison, timestamp validation (missing/invalid/expired/within-window), replay protection (idempotency), payload validation, payload size limit, structured error codes, provider resolution (unknown provider, defaultProvider, verifier map), and the line/stripe/github stub behavior (accurately, as described above).
- After writing the file, RUN it yourself: `cd D:\AI-Workspace\projects\modules-hub\modules\webhook-receiver && npm test`. Fix any failing test that is wrong (a test asserting behavior the code does not have is a WRONG test — correct the test to match real behavior, but NEVER change core/ or providers/). Iterate until npm test passes.

# Report back (verbatim from your final output)
- List the test file path and total test count.
- Report `npm test` real result: N tests passed / M failed, and the pass rate.
- If any genuine production bug was found (a real defect, not a stub), state it explicitly under "PRODUCTION BUGS:". Note that line/stripe/github are stubs is EXPECTED (Stage 2 left them as stubs), not a defect you need to report as a bug — just mention it as a known gap in one line.
